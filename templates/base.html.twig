<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Könyvtár Megnyitó Esemény{% endblock %}</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
    
</head>
<body>
<script src="/js/scripts.js"></script>
    <div class="container">
        <div class="header">
            <h1><i class="fas fa-book-open"></i> Könyvtár Megnyitó</h1>
            <p>Jelentkezz az új könyvtár ünnepélyes megnyitójára!</p>
        </div>

        <div class="card">
            <h2><i class="fas fa-chart-bar"></i> Esemény Állapota</h2>
            <div class="stats" id="statsContainer">
                <div class="stat-item">
                    <div class="stat-number" id="confirmedCount">-</div>
                    <div class="stat-label">Megerősített</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number" id="availableCount">-</div>
                    <div class="stat-label">Szabad hely</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number" id="waitlistCount">-</div>
                    <div class="stat-label">Várólistán</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">50</div>
                    <div class="stat-label">Teljes kapacitás</div>
                </div>
            </div>
        </div>

        <div class="card">
            <h2><i class="fas fa-user-plus"></i> Jelentkezés / Állapot</h2>
            
            <div id="alertContainer"></div>

            <div class="form-group">
                <label for="userName"><i class="fas fa-user"></i> Neved:</label>
                <input type="text" id="userName" class="form-control" placeholder="Add meg a neved..." />
            </div>

            <div class="form-buttons">
                <button class="btn btn-primary" onclick="register()">
                    <i class="fas fa-plus"></i> Jelentkezés
                </button>
                <button class="btn btn-info" onclick="checkStatus()">
                    <i class="fas fa-search"></i> Állapot ellenőrzése
                </button>
                <button class="btn btn-danger" onclick="cancel()">
                    <i class="fas fa-times"></i> Lemondás
                </button>
                <button class="btn btn-info" onclick="loadStats()">
                    <i class="fas fa-sync"></i> Frissítés
                </button>
            </div>

            <div id="statusCard" class="status-card">
                <h3 id="statusTitle"></h3>
                <p id="statusMessage"></p>
            </div>
        </div>
    </div>

    {% block content %}{% endblock %}
</body>
</html>